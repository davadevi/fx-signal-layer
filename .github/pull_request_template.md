## Что сделано
<!-- одна строка -->

## No-lookahead (критично — вручную)
- [ ] Нет `.shift(-N)` и обращений к данным после `cutoff_date`
- [ ] Если затронут бэктест — результат из `reports/` прикреплён

## Тип
- [ ] feature / fix / exp / docs
